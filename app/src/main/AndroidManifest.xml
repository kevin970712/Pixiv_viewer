<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PixivViewer"
        tools:targetApi="31">

        <!-- 主 Activity：處理正常啟動 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.PixivViewer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- 定义我们 App 可以处理的自定义协议 -->
                <data android:scheme="pixiv" android:host="account" android:path="/login" />
            </intent-filter>
        </activity>

        <!-- ✨ 新增：詳情頁 Activity，負責處理作品連結 -->
        <activity
            android:name=".DetailActivity"
            android:exported="true"
            android:theme="@style/Theme.PixivViewer">

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:host="www.pixiv.net" />
                <data android:pathPrefix="/artworks/" />
            </intent-filter>
        </activity>

        <!-- ✨ 新增：作者頁 Activity，負責處理作者連結 -->
        <activity
            android:name=".UserActivity"
            android:exported="true"
            android:theme="@style/Theme.PixivViewer">

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:host="www.pixiv.net" />
                <data android:pathPrefix="/users/" />
            </intent-filter>
        </activity>

        <!-- Settings Activity (保持不變) -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.PixivViewer" />
        <activity
            android:name=".FollowingActivity"
            android:exported="false"
            android:theme="@style/Theme.PixivViewer" />
        <activity
            android:name=".UserBookmarksActivity"
            android.exported="false"
            android:theme="@style/Theme.PixivViewer" />
        <activity
            android:name=".SearchActivity"
            android:exported="false"
            android:theme="@style/Theme.PixivViewer" />
        <activity
            android:name=".ImageViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.PixivViewer" />

    </application>
</manifest>